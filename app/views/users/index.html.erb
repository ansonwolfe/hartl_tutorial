<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
<!-- Rails infers that @users is a list of User objects that when called with a collection of users, Rails automatically iterates through tehm and renders each one with the _user.html.erb partial.    -->
</ul>

<%= will_paginate %>